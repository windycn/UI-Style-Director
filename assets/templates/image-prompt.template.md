# UI Draft Image Prompt

Create a high-fidelity UI draft for:

```text
{{PAGE_GOAL}}
```

Use this style direction:

```text
{{STYLE_INTENT}}
```

Visual style:

```text
{{IMAGE_PROMPT_STYLE}}
```

Constraints:

```text
{{CONSTRAINTS}}
```

Avoid:

```text
{{AVOID}}
```

Output should look like a real product screen, not a marketing mockup unless the page is explicitly a landing page.
